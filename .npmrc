# @flower-ai scope 走公网 npm registry(默认值,这里只是显式声明便于 grep)
@flower-ai:registry=https://registry.npmjs.org/

# npm 鉴权 token 放在 ~/.npmrc 或 CI 的 secret 里,不要写到这里:
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
