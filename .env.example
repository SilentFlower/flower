# =============================================================================
# LLM 网关
# =============================================================================
# LLM 网关 baseUrl,会被 pi-providers 注册(可指向自部署 vLLM / 内部 AI Gateway / OpenAI 代理 等)
COMPANY_LLM_BASE_URL=https://ai-gateway.corp.internal/v1
# LLM 网关的 API key
COMPANY_AI_TOKEN=

# =============================================================================
# GitLab(code-reviewer 使用)
# =============================================================================
# GitLab 个人/项目 token,用于读 MR diff、发评论
GITLAB_TOKEN=
# GitLab 实例地址
GITLAB_HOST=https://gitlab.corp.internal
# CI 注入的变量(运行时由 GitLab 自动填充)
# CI_PROJECT_ID=
# CI_MERGE_REQUEST_IID=
# CI_COMMIT_SHA=

# =============================================================================
# 钉钉(ops-bot 使用)
# =============================================================================
# 钉钉企业内部应用 AppKey / AppSecret
DINGTALK_APP_KEY=
DINGTALK_APP_SECRET=
# 钉钉机器人加签密钥(防伪)
DINGTALK_BOT_SECRET=

# =============================================================================
# 阿里云 ARMS / SLS(ops-bot 使用)
# =============================================================================
ALICLOUD_AK=
ALICLOUD_SK=
# 默认 region
ALICLOUD_REGION=cn-hangzhou

# =============================================================================
# 会话存储
# =============================================================================
REDIS_URL=redis://localhost:6379

# =============================================================================
# 审计 / SIEM
# =============================================================================
# 审计上报地址(SIEM / 自定义 HTTP 接收端,留空则不上报)
SIEM_INGEST_URL=
