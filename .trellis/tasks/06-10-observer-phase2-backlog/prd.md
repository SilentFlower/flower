# flower-observer 二期 backlog(范围待定)

## Goal

一期(06-10-flower-observer)明确排除的二期候选,立项占位、范围待评审筛选,不必全做:GitLab webhook + artifact 自动补拉(seq 缺口修复通道,httpSink 任务 ADR 留的降级路径)/ SSE 实时推流(现为详情页 30s 轮询)/ 全文搜索 / min-max 时长过滤 / 列表侧栏预览 / 完整 Gantt 瀑布(span 无 parent_id,需评估)/ 转发真实公司 SIEM(observer 加 forward)/ 多租户权限与登录体系

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
